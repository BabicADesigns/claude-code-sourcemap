# Architecture Overview

_Generated by [`scripts/generate-knowledge-graph.mjs`](scripts/generate-knowledge-graph.mjs) via static analysis of `src/` — no LLM involved, regenerate any time with `node scripts/generate-knowledge-graph.mjs`._

This pass scanned **211 files** and resolved **896 local import edges** under `src/`.

## Top-level modules

| Directory | Files | Role |
| --- | --- | --- |
| `src/components` | 71 | Reusable Ink/React components shared across screens and commands. |
| `src/utils` | 38 | Shared utilities: config, git, logging, permissions, shell, markdown, etc. |
| `src/tools` | 35 | Agent tool implementations (Bash, file read/edit/write, grep, glob, agent, ...). |
| `src/commands` | 19 | Implementations of `/slash` commands (config, cost, init, login, review, ...). |
| `src/hooks` | 14 | React hooks for terminal/input state (text input, key history, timers, ...). |
| `src/services` | 10 | External integrations: Claude API client, MCP client, OAuth, Sentry, Statsig. |
| `src/(root)` | 9 | — |
| `src/constants` | 8 | Static strings/config: prompts, product name, ASCII art, feature betas. |
| `src/screens` | 5 | Full-screen Ink views (REPL, Doctor, log list, resume picker, npm-prefix setup). |
| `src/entrypoints` | 2 | CLI bootstrap (`cli.tsx`) and the MCP server entrypoint (`mcp.ts`). |

## Directory dependency graph

Edges below are aggregated from file-level imports (self-loops within a directory omitted); thicker textual weight = more underlying import edges.

```mermaid
flowchart LR
  components -->|128| utils
  tools -->|61| utils
  components -->|35| hooks
  services -->|32| utils
  (root) -->|28| utils
  commands -->|28| (root)
  components -->|24| tools
  tools -->|24| components
  commands -->|22| utils
  components -->|21| (root)
  (root) -->|21| tools
  entrypoints -->|20| utils
  (root) -->|19| commands
  screens -->|19| utils
  components -->|17| services
  screens -->|14| components
  utils -->|13| services
  components -->|11| constants
  hooks -->|10| utils
  entrypoints -->|8| tools
  screens -->|8| (root)
  utils -->|8| (root)
  entrypoints -->|7| (root)
  tools -->|7| services
  constants -->|6| utils
  hooks -->|6| services
  screens -->|6| hooks
  tools -->|6| (root)
  commands -->|5| components
  hooks -->|5| (root)
  (root) -->|4| services
  entrypoints -->|4| services
  entrypoints -->|4| screens
  entrypoints -->|4| components
  screens -->|4| services
  services -->|4| (root)
  services -->|4| constants
  utils -->|4| constants
  commands -->|3| constants
  hooks -->|3| components
  screens -->|3| constants
  tools -->|3| constants
  commands -->|2| screens
  entrypoints -->|2| constants
  entrypoints -->|2| commands
  (root) -->|2| hooks
  services -->|2| components
  utils -->|2| components
  utils -->|2| tools
  (root) -->|1| constants
  commands -->|1| services
  commands -->|1| hooks
  commands -->|1| tools
  components -->|1| commands
  constants -->|1| tools
  hooks -->|1| tools
  hooks -->|1| screens
  (root) -->|1| components
  services -->|1| tools
  utils -->|1| hooks
```

## Most-depended-on modules (fan-in)

Files imported from the largest number of other files — a rough proxy for "core" modules worth understanding first.

| File | Imported by N files |
| --- | --- |
| `src/utils/theme.ts` | 61 |
| `src/utils/log.ts` | 55 |
| `src/utils/state.ts` | 38 |
| `src/services/statsig.ts` | 35 |
| `src/utils/config.ts` | 33 |
| `src/commands.ts` | 28 |
| `src/query.ts` | 27 |
| `src/utils/messages.tsx` | 25 |
| `src/utils/env.ts` | 22 |
| `src/constants/product.ts` | 15 |
| `src/utils/model.ts` | 15 |
| `src/services/claude.ts` | 14 |
| `src/hooks/useTerminalSize.ts` | 14 |
| `src/tools/BashTool/BashTool.tsx` | 13 |
| `src/components/FallbackToolUseRejectedMessage.tsx` | 13 |

## Tool catalog

Each entry under `src/tools/` is a self-contained agent tool. Descriptions are the first line of each tool's own `DESCRIPTION` constant (the same text the model sees).

| Tool | Description |
| --- | --- |
| `AgentTool` | — |
| `ArchitectTool` | Your go-to tool for any technical or coding task. Analyzes requirements and breaks them down into clear, actionable implementation steps. Use this whenever you need help planning how to implement a feature, solve a technical problem, or structure your code. |
| `BashTool` | — |
| `FileEditTool` | This is a tool for editing files. For moving or renaming files, you should generally use the Bash tool with the 'mv' command instead. For larger edits, use the Write tool to overwrite files. For Jupyter notebooks (.ipynb files), use the ${NotebookEditTool.name} instead. |
| `FileReadTool` | Read a file from the local filesystem. |
| `FileWriteTool` | Write a file to the local filesystem. |
| `GlobTool` | - Fast file pattern matching tool that works with any codebase size |
| `GrepTool` | - Fast content search tool that works with any codebase size |
| `lsTool` | Lists files and directories in a given path. The path parameter must be an absolute path, not a relative path. You should generally prefer the Glob and Grep tools, if you know which directories to search. |
| `MCPTool` | — |
| `MemoryReadTool` | — |
| `MemoryWriteTool` | — |
| `NotebookEditTool` | Replace the contents of a specific cell in a Jupyter notebook. |
| `NotebookReadTool` | Extract and read source code from all code cells in a Jupyter notebook. |
| `StickerRequestTool` | Sends the user swag stickers with love from Anthropic. |
| `ThinkTool` | This is a no-op tool that logs a thought. It is inspired by the tau-bench think tool. |

## Regenerating

```bash
node scripts/generate-knowledge-graph.mjs
```

This rewrites `knowledge-graph.json` (full file-level node/edge list) and this document.
