# Copy to .env and fill in your own key (never commit the real value).
# Used by .mcp.json to authenticate the Tavily MCP server (search, extract, crawl, map).
# Get a key at https://app.tavily.com
TAVILY_API_KEY=
